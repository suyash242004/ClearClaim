# ClearClaim frontend — environment template
# Copy to .env (or set in Vercel/Netlify project settings).

# Deployed AI agent gateway (Render). Falls back to http://127.0.0.1:8000 for local dev.
VITE_AGENT_API_URL=https://your-agents-service.onrender.com
